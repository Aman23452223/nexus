fastapi>=0.110
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
httpx>=0.27
# Local-only (laptop agent, NOT deployed): uvicorn, playwright
# pip install uvicorn playwright; python -m playwright install chromium
